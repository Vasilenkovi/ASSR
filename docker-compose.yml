version: '3.8'
services:
  db:
    build: ./PostgreSQL
    ports:
      - 5432:5432
  app:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
  nginx:
    build: ./nginx
    ports:
      - 80:80
